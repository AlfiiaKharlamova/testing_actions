name: Applying features
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Start applying features into Feast"
      - run: cd ./in_bug
      - run: chmod +x applyScript.sh
      - run: ./applyScript.sh
      - run: echo "🍏 This job's status is ${{ job.status }}."