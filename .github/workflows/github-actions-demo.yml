name: Applying features
on: [push]
jobs:
  Apply feast:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Start applying features into Feast"
      - name: Cd to dir
        run: |
          cd /home/alfia/Testing_feast/testing_actions/in_bug
      - name: Applying feast
        run: |
          feast apply
      - run: echo "🍏 This job's status is ${{ job.status }}."